<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>@Page.Title</title>
        <link href="~/Content/Site.css" rel="stylesheet" />
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <script src="~/Scripts/modernizr-2.5.3.js"></script>
        @RenderSection("head", required: false)
    </head>
    <body>
        <div id="page" class="group">
            <header>
                <p class="title">Nick Austin Lee</p>
                <p>This is a beta version of my personal site. I am experimenting with Windows Azure. Nevertheless, feel free
                to click around and learn about what I'm up to!</p>
                <nav>
                    <ul>
                        @foreach (var contentPage in App.ContentPages) {
                            if (contentPage.Title == Page.Title) {
                                <li><a href="~/ContentPage/@contentPage.Id" title="@contentPage.Header" class="current">@contentPage.Header</a></li>
                            } else {
                                <li><a href="~/ContentPage/@contentPage.Id" title="@contentPage.Header">@contentPage.Header</a></li>
                            }
                        }
                    </ul>
                </nav>
            </header>
            <section>
                @RenderBody()
            </section>
        </div>
    </body>
</html>